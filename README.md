# Base Minimal Contracts

A collection of 14 ultra-minimal Solidity contracts deployed on Base Mainnet.

## Setup

npm install

## Configure

Add BASE_RPC and PRIVATE_KEY in .env

## Deploy

npx hardhat run scripts/deploy.js --network base
